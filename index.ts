import { Config } from 'tailwindcss';

const preset: Config = {
    content:[],
    theme: {
        extend: {
            fontFamily: {
                sans: ['Manrope', 'ui-sans-serif', 'system-ui'],
            },
        },
    },
    plugins: [],
};

export default preset;
